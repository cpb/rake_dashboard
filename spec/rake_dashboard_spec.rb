require 'spec_helper'

describe RakeDashboard do
  it 'has a version number' do
    expect(RakeDashboard::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
